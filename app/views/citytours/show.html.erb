<div class="tour-show-container">
  <div class="d-flex justify-content-between" style="margin-top: 16px;">
    <div class="citytour-details" style="width: 30%;">
      <%#   C I T Y T O U R - D E T A I L S  %>
      <div class="p-2">
        <div class="tour-show-card" style="width: 100%;">
          <h1><%= @citytour.name  %></h1><br>
          <p><%= @citytour.overview %></p>
          <p>Guide: <%= @citytour.guide.name %></p><br>
          <div class="d-flex price-book">
          <p class="price-text">$ <%= @citytour.price %></p>
          <% if current_user == @citytour.guide %>
            <div class="d-flex p-2">
              <div style="margin: 10px 0 0 10px;;">
                <div class="btn btn-warning">
                  <%= link_to "←", citytours_path %>
                </div>
                <div class="btn btn-warning">
                <%= link_to "Edit", edit_citytour_path(@citytour) %>
                </div>
                <div class="btn btn-warning">
                  <%= link_to "🗑️", @citytour, method: :delete %>
                </div>
              </div>
            </div>
          <% else %>
          <%= link_to "📖 Book", new_citytour_booking_path(@citytour), class: "btn button-primary2"%>
          <% end %>
          </div>
        </div>
      </div>
      <%#   M A P B O X   %>
      <div class="p-2">
        <div d="mapbox_sights">
          <div style="height: 280px;"
            data-controller="map"
            data-map-center-value="[-0.118092,51.509865]"
            data-map-markers-value="<%= @sightmarkers.to_json %>"
            data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
          </div>
        </div>
      </div><%# close d-flex %>
      <%#   L I N K S   %>
    </div>
    <div class="citytour_sights_photos">
      <%#   S I G H T  %>
      <div class="sights-carousel-container">
        <div class="the_card" style="width: 100%; object-fit: cover; height: 100%;">
          <%= render 'sights/carousel', sights: @citytour.sights %>
        </div>
      </div>
      <%#   C I T Y   T O U R S   %>
      <div class="d-flex p-2 w-100">
        <div data-controller="insert-in-list" class="w-100">
          <div data-insert-in-list-target="items">
            <%# @citytours.each do |citytour| %>
              <%= render 'reviews/carousel', reviews: @citytour.reviews %> <%#= .where(isguide: false) %>
            <%# end %>
            <p style="color: green"><%#= notice %></p>
          </div>
        </div>
      </div><%# close d-flex p-2 %>
    </div>

  </div>









<%# MELCHIOR %>
                        <%# if current_user.guide? %>
                        <%# <div class="btn btn-warning"> %>
                             <%#= link_to "Go to sights ", sights_path %>
                                <%#= button_to "Destroy this sight", @sight, method: :delete %>
                          <%# </div> %>
                        <%# end %>
  <%# MELCHIOR %>



  </div> <%# closed-flex align-content-center flex-wrap %>
</div> <%# close Container %>
